#include <stdio.h>

int T, N;
char nums[10];

char a[4];
int main() {

	a = "same";

}

/*
int result;
int main() {
	freopen("Text.txt", "r", stdin);

	scanf("%d", &T);

	for (int tc = 0; tc < T; tc++) {
		scanf("%d", &N);
		for (int i = 0; i < N; i++) {
			scanf("%d", &nums[i]);
		}
		result = nums[0];
		for (int i = 1; i < N; i++) {
			if (result < 2) {
				result += nums[i];

			}
			else if (nums[i] < 2) {
				result += nums[i];
			}
			else {
				result *= nums[i];
			}
		}
		printf("#%d %d\n", tc + 1, result);
	}




	return 0;
}*/